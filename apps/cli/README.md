# AI Privacy Firewall CLI

The CLI scans content locally. It does not send prompts, file contents, detections, or redaction maps to a backend.

Build it from the repository root with `pnpm --filter @privacy-guard/cli build`, then run `node apps/cli/dist/cli.cjs --help`.
